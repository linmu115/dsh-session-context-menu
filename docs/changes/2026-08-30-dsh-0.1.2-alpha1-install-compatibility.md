# 0.2.1 — DSH 0.1.2-alpha.1 installation compatibility

The optional Workspace UI peer uses `*`, so package installation no longer rejects alpha or future experimental DSH versions. The plugin continues to discover the Workspace service at runtime.

This release does not claim a migration from a removed alpha.1 API; it removes only the package-version admission gate.
