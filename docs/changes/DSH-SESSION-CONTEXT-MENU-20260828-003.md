# Version-open workspace peer

`dsh-session-context-menu@0.2.1` changes its host workspace peer to `*`. The workspace service is still optional and discovered at runtime, but package installation no longer imposes a DSH version range.

Development and test dependencies remain pinned independently.
