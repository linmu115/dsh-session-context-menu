# 0.2.2 — DSH 0.1.2-rc.1 rebuild

- Rebuilt the existing context-menu integration for DSH `0.1.2-rc.1`.
- Kept the workspace peer open and retained the current public Workspace and
  Session controller calls. RC1 does not require a production API shim here.
- DOM integration remains intentionally narrow and will only be changed if a
  focused RC1 mount breakpoint shows a concrete host markup regression.
